[project]
name = "atp-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "filelock>=3.13.1",
    "jsonschema>=4.21.1",
    "mcp>=0.1.0",
]

[project.scripts]
atp-server = "main:mcp.run"
